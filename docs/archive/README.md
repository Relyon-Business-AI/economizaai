# Archived docs

Historical documents kept for reference but **no longer current**. They served their
purpose and were moved out of the active doc set (2026-09-22 doc cleanup).

- **CPF_AUTOIMPORT.md** — a jul/2026 spike exploring auto-importing receipts via CPF.
  Conclusion was "viável, próximo candidato", but the feature was never built. Useful
  reference if CPF auto-import is revisited.
- **FE_CLAUDE_REPORT.md** — a jun/2026 handoff snapshot for the frontend Claude. The live
  contract is now `API.md` + `CHANGELOG.md`; this is a point-in-time record.

For current docs see the repo root (`HELP.md`, `DEV_NOTES.md`, `API.md`, `MONETIZATION.md`,
`INFRASTRUCTURE.md`, `RENDER_SETUP.md`, `OPERATOR.md`) and `docs/`.
